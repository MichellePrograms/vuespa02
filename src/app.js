import Vue from 'vue'

const app = new Vue({
  data: {
    hello: 'working on two'
  }
  //dont use template yet cuz it is just fillingin the hello on the index.html page right now!!
  // breaks it
  //},
  //template: '<div id="app">{{ hello }}</div>'
})

export { app }


